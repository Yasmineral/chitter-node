
    <div class="my-title-container">
      <h3 class="my-title">New round here? Sign up!</h3>
      </div>
  <main>
    {{#each errors}}
      {{#each this}}
        {{#is name "Invalid value"}}
        {{else}}
          <p>{{this.msg}}</p>
        {{/is}}
      {{/each}}
    {{/each}}
    <form class="chitter-form" action="/register" method="POST">
      <label for="name">Name</label>
      <input class="u-full-width" type="text" id="name" name="name">
      <label for="content">Email</label>
      <input class="u-full-width" type="text" id="email" name="email">
      <label for="content">Password</label>
      <input class="u-full-width" type="text" id="password1" name="password1">
      <label for="content">Confirm password</label>
      <input class="u-full-width" type="text" id="password2" name="password2">
      <button class="button-primary">Post</button>
    </form>
  </main> 

